# Face Shield UI — loaded by Vite.
# With `npm run dev`, leave VITE_API_URL unset so requests use /api (proxied to :8000).
# Uncomment if you use `vite preview` or the proxy cannot reach the backend:
# VITE_API_URL=http://127.0.0.1:8000
